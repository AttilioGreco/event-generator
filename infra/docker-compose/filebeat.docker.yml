filebeat.inputs:
      # - type: syslog
      #   format: rfc5424
      #   protocol.tcp:
      #         host: "0.0.0.0:9000"

      # - type: syslog
      #   format: rfc5424
      #   protocol.udp:
      #         host: "0.0.0.0:9000"

      - type: http_endpoint
        enabled: true
        listen_address: "0.0.0.0"
        listen_port: 9080
        content_type: "application/json"
        response_code: 200
        response_body: '{"message": "success"}'
        url: "/"
        prefix: "json"
        basic_auth: true
        username: admin
        password: secret

output.console:
      pretty: true
